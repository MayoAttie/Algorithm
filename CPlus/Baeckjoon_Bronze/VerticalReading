#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main()
{
    string arr[5];
    vector<char> answer;

    for(int i=0; i<5; i++)
    {
        cin >> arr[i];
    }
    int len;
    for(int i=0; i<5; i++)
    {
        len = max(len, arr[i].length());
    }

    for(int i=0; i<len; i++)
    {\
}
}