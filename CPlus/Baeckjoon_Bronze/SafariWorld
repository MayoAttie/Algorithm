#include <iostream>
#include <cstdlib>
using namespace std;
int main() 
{
    long long N, M;
    cin >> N >> M;
    long long difference = abs(N - M);
    cout << difference << endl;

    return 0;
}