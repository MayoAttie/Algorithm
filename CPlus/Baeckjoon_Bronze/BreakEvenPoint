#include <iostream>
using namespace std;

int main()
{
    long long A, B, C;
    cin >> A >> B >> C;

    if (B >= C) 
        cout << -1 << endl;
    else 
    {
        long long cost = A / (C - B) + 1;
        cout << cost << endl;
    }

    return 0;
}