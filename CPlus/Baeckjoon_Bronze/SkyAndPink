#include <iostream>
#include <vector>
using namespace std;

int main() 
{
    int N;
    cin >> N;
    vector<int> A(N);
    vector<int> B(N);

    long long sumA = 0;
    long long sumB = 0;

    for (int i = 0; i < N; ++i) 
    {
        cin >> A[i];
        sumA += A[i];
    }
    for (int i = 0; i < N; ++i) 
    {
        cin >> B[i];
        sumB += B[i];
    }

    cout << sumB << " " << sumA;

    return 0;
}