#include <iostream>	
#include <vector>	

using namespace std;

int main()
{
	int n, x;
	cin >> n >> x;

	int sum = 0;
	for (int i = 0; i < n; i++)
	{
		int num;
		cin >> num;
		sum += num;
	}

	cout << (sum % x == 0) ? 1 : 0;

}