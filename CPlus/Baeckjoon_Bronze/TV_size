#include <iostream>
#include <cmath>
using namespace std;

int main() 
{
    int D, H, W;
    cin >> D >> H >> W;

    double ratio = sqrt(H * H + W * W); 
    int h = (H * D) / ratio;
    int w = (W * D) / ratio;

    cout << h << " " << w;

    return 0;
}