#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;


const double PI = acos(-1.0);

int main() 
{
    int R;
    cin >> R;
    cout << fixed << setprecision(6) << PI * pow(R, 2) << endl;
    cout << fixed << setprecision(6) << R * R * 2 << endl;
    return 0;
}