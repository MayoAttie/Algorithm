#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

int main()
{
	int N;
	cin >> N;

	long long answer = 0;
	for (int i = 1; i <= N; i++)
	{
		answer += (N / i) * i;
	}
	cout << answer;
}