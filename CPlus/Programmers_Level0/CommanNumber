#include <string>
#include <vector>

using namespace std;

bool isCd(int number, int a, int b)
{
    if(number%a == 0 && number%b == 0)
        return true;
    else
        return false;
}

int solution(int number, int n, int m) 
{
    return isCd(number, n, m);
}