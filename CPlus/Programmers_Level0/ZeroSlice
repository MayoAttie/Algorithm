#include <string>
#include <vector>

using namespace std;

string solution(string n_str) 
{
    string answer = "";
    bool isAdd = false;
    for(char ch : n_str)
    {
        if(ch != '0')
            isAdd = true;
        if(isAdd)
            answer += ch;
    }
    return answer;
}