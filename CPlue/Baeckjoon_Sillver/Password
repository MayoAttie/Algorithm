#include <iostream>
#include <vector>
#include <queue>
using namespace std;

vector<vector<int>> graph;
vector<vector<bool>> visited;
vector<pair<int,int>> targets;

int drY[8] = {2,  1,2,1,-2,-1,-2,-1};
int drX[8] = {-1,-2,1,2,-1,-2, 1, 2};

int main()
{
    int N, M;
    cin >> N >> M;

    pair<int,int> start;
    cin >> start.first >> start.second;

    graph = vector<vector<int>>(N,vector<int>(N, 0));
    visited = vector<vector<bool>>(N, vector<bool>(N, false));
    targets = vector<pair<int,int>(M);

    for(int i=0; i<M; i++)
    {
        cin >> targets[i].first >> targets[i].second;
    }
    
}

void BFS(pair<int,int> start, )